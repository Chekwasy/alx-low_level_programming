#include "main.h"
/**
 * islower - Check description
 * Description: It prints the alphabet in lowercase fallowe\
 * d by a new line
 * Return: Nothing.
 */
int islower(int c)
{
int res;

if (islower())
{
	res = 1;
}
else
{
	res = 0;
}
return (res);
}
